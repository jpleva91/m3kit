# Contestant Entry: Claude Code / fable

## Status
DNF / degraded lane.

## Runtime evidence
DNF: attempted with `HOME=/home/red claude --model fable -p --allowedTools Read --max-turns 4 --no-session-persistence`; process hung with no stdout until killed with the orchestration script, so it did not produce an evidence-backed packet.

## Score
0/30. No evidence-backed feature packet was available to score.
