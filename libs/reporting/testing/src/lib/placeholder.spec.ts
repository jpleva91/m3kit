import { REPORTING_TESTING_PLACEHOLDER } from './placeholder';

describe('reporting-testing placeholder', () => {
  it('exports the named placeholder token', () => {
    expect(REPORTING_TESTING_PLACEHOLDER).toBe('reporting-testing');
  });
});
