import { REPORTING_MATERIAL_PLACEHOLDER } from './placeholder';

describe('reporting-material placeholder', () => {
  it('exports the named placeholder token', () => {
    expect(REPORTING_MATERIAL_PLACEHOLDER).toBe('reporting-material');
  });
});
