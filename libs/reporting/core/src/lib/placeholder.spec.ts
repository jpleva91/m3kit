import { REPORTING_CORE_PLACEHOLDER } from './placeholder';

describe('reporting-core placeholder', () => {
  it('exports the named placeholder token', () => {
    expect(REPORTING_CORE_PLACEHOLDER).toBe('reporting-core');
  });
});
