// @m3kit/core is pure TypeScript (FR-012): its Vitest specs exercise plain
// functions and types only, so no test environment bootstrap is required.
// This file is intentionally a no-op; it exists because the project's
// vite.config.mts and tsconfig.spec.json reference a shared setup entry.
export {};
