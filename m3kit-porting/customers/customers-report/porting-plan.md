# m3kit Porting Plan: customers/customers-report

Target: `apps/demo-reporting/src/app/reports/customers-report.component.ts`

Project: `demo-reporting`

Recommended lift command:

```sh
npx nx g @m3kit/plugin:lift --libs=shell,forms,table,feedback,state --scope=ui
```

Generate side-by-side scaffold only; do not delete or rewrite the source page.
