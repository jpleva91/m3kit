# m3kit-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build m3kit-plugin` to build the library.

## Running unit tests

Run `nx test m3kit-plugin` to execute the unit tests via [Jest](https://jestjs.io).
