import { Component } from '@angular/core';

/**
 * Placeholder route component for the scaffold phase.
 * The reporting feature (report table, filter bar) arrives in a later
 * phase, after the clean-room review gate, built on libs/reporting/*.
 */
@Component({
  selector: 'app-reports',
  template: `
    <h1>Reports</h1>
    <p>Reporting features land here in a later phase.</p>
  `,
})
export class ReportsComponent {}
